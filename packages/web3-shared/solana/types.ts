export enum ChainId {
    MainnetBeta = 1,
    Testnet = 2,
    Devnet = 3,
}

export enum NetworkType {
    Solana = 'Solana',
}

export enum ProviderType {
    Phantom = 'Phantom',
    Sollet = 'Sollet',
}
