export enum ChainId {
    Mainnet = 1,
    Testnet = 2,
}

export enum NetworkType {
    Flow = 'Flow',
}

export enum ProviderType {
    Blocto = 'Blocto',
    Dapper = 'Dapper',
    Ledger = 'Ledger',
}
