import { story } from '../utils'
import { SendingCodeFieldExample } from './SendingCodeFieldExample'

const { meta, of } = story(SendingCodeFieldExample)
export default meta({
    title: 'Atoms/SendingCodeField',
    parameters: {},
})

export const SendingCodeField = of({})
