import { story } from '../../utils'
import { SearchableListExample } from './SearchableListExample'

const { meta, of } = story(SearchableListExample)
export default meta({
    title: 'SharedComponents/SearchableList',
    parameters: {},
})

export const SearchableList = of({})
