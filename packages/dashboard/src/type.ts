export { DashboardRoutes as RoutePaths } from '@masknet/shared'
export { DashboardRoutes } from '@masknet/shared'
export { TradeProvider } from '@masknet/public-api'
export enum MarketTrendProvider {
    COIN_GECKO = 0,
    COIN_MARKET_CAP = 1,
    UNISWAP_INFO = 2,
}
