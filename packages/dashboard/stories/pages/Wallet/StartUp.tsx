import { story } from '@masknet/storybook-shared'
import { StartUp as C } from '../../../src/pages/Wallets/StartUp'
const { meta, of } = story(C)

export default meta({ title: 'Pages/Wallet/Start Up' })

export const StartUp = of({})
