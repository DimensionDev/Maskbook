import * as React from 'react'
import { Stack } from '@mui/material'

interface PreviewCardProps {
    url: string
}

export const PreviewCard = ({ url }: PreviewCardProps) => {
    return <Stack>Test</Stack>
}
