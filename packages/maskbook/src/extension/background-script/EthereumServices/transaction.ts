import type { EthereumTransactionConfig } from '@masknet/web3-shared'

export async function createCancelTransaction(tx: EthereumTransactionConfig) {
    // TODO
}

export function createSpeedUpTransaction(tx: EthereumTransactionConfig) {
    // TOOD
}

export function parseTransaction(tx: EthereumTransactionConfig) {}
