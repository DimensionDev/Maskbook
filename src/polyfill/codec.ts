; (function() {
    // TODO: TextEncoder
    if (typeof TextEncoder === undefined) {
        throw new Error("Browser doesn't support `TextEncoder`");
    }

    // TODO: TextDecoder
    if (typeof TextEncoder === undefined) {
        throw new Error("Browser doesn't support `TextDecoder`");
    }
})();
