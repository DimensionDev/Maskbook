name: Linters and tests

on:
  push:
    branches: [master, develop, released]
  pull_request:
    branches: [master, develop, released]

jobs:
  prettier:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-node@v2-beta
      - run: npx prettier --list-different .
  locale-kit:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-node@v2
      - uses: pnpm/action-setup@master
        with:
          version: latest
      - uses: actions/cache@v2
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-node-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-node-
      - run: pnpm install
      - run: npx gulp locale-kit
  type-check:
    runs-on: ubuntu-20.04
    steps:
      - name: Get cache date
        id: get-date
        run: |
          echo "::set-output name=date::$(/bin/date -u "+%Y%m%d")"
        shell: bash
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-node@v2
      - uses: pnpm/action-setup@master
        with:
          version: latest
      - uses: actions/cache@v2
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-node-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-node-
      - uses: actions/cache@v2
        with:
          path: "packages/**/dist/"
          # actions/cache will not upload changes in cache if primary key hits
          # by adding date to the primary key, we can ensure the cache updates on the first build of the day
          key: ${{ runner.os }}-tsc-${{ hashFiles('packages/**/tsconfig.json') }}-${{ hashFiles('pnpm-lock.yaml') }}-${{ steps.get-date.outputs.date }}
          # Allow fallback to cache of different dependencies but not allowing fallback to different tsconfig
          # because that might indicates a structural/flags changes in tsc emit.
          restore-keys: |
            ${{ runner.os }}-tsc-${{ hashFiles('packages/**/tsconfig.json') }}-${{ hashFiles('pnpm-lock.yaml') }}
            ${{ runner.os }}-tsc-${{ hashFiles('packages/**/tsconfig.json') }}
      - run: pnpm install
      - run: npx gulp codegen
  eslint:
    runs-on: ubuntu-20.04
    needs: [prettier, locale-kit, type-check]
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-node@v2
      - uses: pnpm/action-setup@master
        with:
          version: latest
      - uses: actions/cache@v2
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-node-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-node-
      - run: pnpm install
      - run: npx eslint . --ext .ts,.tsx,.js --cache --max-warnings=0
        working-directory: packages
      - run: npx eslint . --ext .ts,.tsx,.js --cache --format junit -o reports/junit/eslint-results.xml
        working-directory: packages
      - name: Upload eslint report
        uses: actions/upload-artifact@v2
        with:
          name: eslint
          path: reports/junit
